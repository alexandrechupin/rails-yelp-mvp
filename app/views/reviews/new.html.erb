
<div class="container mt-4">

  <h1> Add a new review </h1>

  <%= simple_form_for [@restaurant, @review] do |f| %>
    <%= f.input :content %>
    <%= f.input :rating, collection: Review::SCALE %>
    <%= f.submit "Add a new review", class: "btn btn-primary" %>
  <% end %>

</div>
